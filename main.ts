import { GameBoard } from './components/GameBoard';

const gameBoard = new GameBoard();

function initGame() {
    gameBoard.render();
}

function handleUserInteraction() {
    // Logic to handle user interactions will go here
}

initGame();